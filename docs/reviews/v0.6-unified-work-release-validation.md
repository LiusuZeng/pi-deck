# v0.6 Unified Work Release Validation

Date: 2026-08-24
Validated product candidate: `29cb814fa35e50d2fb92b870a02cb8d5a376fc47`
Evidence branch: `docs/v0.6-final-review-evidence`
Version: `0.6.0`
Base release: `v0.5.5` (`5c5d733c084f98e2ffb90336fc55bcac4f62bd7c`)

Validation ran against the exact committed product candidate `29cb814fa35e50d2fb92b870a02cb8d5a376fc47`
through the combined `npm run verify:release` gate. Ignored installed
dependencies and generated build/test outputs were not part of the validated
Git tree.

## Environment

- macOS 26.5.2 (25F84)
- Node.js v26.0.0
- npm 11.12.1
- Pi 0.81.1 with authenticated provider configuration

## Release gate

Command:

```bash
npm run verify:release
```

Result: **passed**.

### Immutable execution artifact

The normalized, committed [candidate-bound `verify:release` log](artifacts/v0.6-29cb814-verify-release.log)
is evidence for the product candidate, not for this documentation commit.

- SHA-256 (normalized artifact):
  `26ace3cbb83019006d8898c3d51667f35a6c2a1d94a1f58d71ee92f9f7bc1485`
- Validation timestamp (UTC): `2026-08-24T16:33:39Z`
- Product branch: `feature/v0.6-unified-work`
- Working tree before validation: empty (`git status --short` produced no output)
- Command: `npm run verify:release`
- Working tree after validation: empty (`git status --short` produced no output)
- Counts: unit/integration 74 files passed, 593 tests passed, 2 todo;
  deterministic Electron E2E 61 passed, 6 skipped; authenticated real-Pi E2E
  6 passed.

The artifact is a display-control-normalized copy of the source execution log;
it preserves its semantic output. It was reviewed for secrets before commit; no
secrets were found.

- Formatting, TypeScript, main/preload/renderer production builds, and site
  validation passed for v0.6.0.
- Unit/integration: 74 files passed; 593 tests passed; 2 todo.
- Deterministic Electron E2E: 61 passed. Its six authenticated real-Pi tests
  were intentionally skipped during the deterministic phase and run separately
  below.

The Electron suite included Unified Work launch/scope/origin/ownership/runtime
coverage, cross-workspace canonical sidebar resume, workspace lifecycle,
workflow coexistence, private task exclusion, default-only progressive
disclosure and global ownership, removal of Close Work, waiting-input mutation
protection, fake workspace draft backend ownership, retained per-scope Work
filters/focus/scroll context, parent-title recovery from Pi `session_info`,
and the v0.5.5 tooltip-hover stabilization.

`verify:release` runs the deterministic CI-equivalent phase followed by the
separate authenticated real-Pi phase.

## Authenticated real-Pi gate

Command:

```bash
npm run test:e2e:real-smoke
```

Result: **passed — 6/6**.

The passing scenarios covered:

1. Parallel worker-model selection.
2. Agent Workflow persistence across restart.
3. Default-workspace prompt/resume and explicit legacy bridge compatibility.
4. Ordinary parent sessions omitting the legacy `deck_delegate` tool.
5. Draft Parallel opt-in routing from the first prompt.
6. Production planning of multiple private task sessions without bridge
   election.

No test was skipped in the authenticated command. The production Parallel
scenario also proved that planner/task workers left exactly one durable parent
JSONL, that the parent's canonical identity and original title survived
synthesis and restart, and that sidebar, All Work, and scoped Work each exposed
only that parent.

## Documentation captures

Before final validation, the candidate's current product screenshots were
regenerated and reviewed:

```bash
npm run docs:capture
PI_DECK_CAPTURE_REAL_PI=1 npm run docs:capture:real-workflows
npm run docs:render-social-preview
npm run check:site
```

The deterministic capture is accurately documented as real-mode UI driven by
production-shaped fake Pi RPC fixtures. Workflow and Parallel screenshots used
real local Pi workers. Mirrored `docs/assets` and `site/assets` files were
checked by the release-doc pass.

## Feedback disposition

- P1's observed synthesis row was the canonical parent mislabeled by synthesis,
  not a leaked child session. It is fixed structurally through `session_info`,
  with one-parent JSONL proof and no heuristic cleanup.
- Work filter state, focus, and scroll context are retained per scope. A stable
  row restores meaningful mouse offset and keyboard focus; a missing row resets
  and focuses the visible heading.

This records implementation and validation evidence only; it does not assert
reviewer approval or tag validation.

## Release ancestry

The candidate descends from the two-parent merge of the prior v0.6 integration
tip and the released `v0.5.5` commit.
`git merge-base --is-ancestor v0.5.5 29cb814` passes, so v0.6 contains the
tagged release's actual history rather than replayed patches.

## Scope note

This evidence record is committed after the validated product candidate and
changes only release documentation. The product, tests, release metadata, and
asset tree validated above are those of `29cb814fa35e50d2fb92b870a02cb8d5a376fc47`;
this documentation commit is distinct from that candidate.
