# v0.6 Unified Work Release Validation

Date: 2026-08-23  
Validated candidate: `f71f21190b003741919cbc632de4cbdea85e046a`
Branch: `feature/v0.6-unified-work`  
Version: `0.6.0`
Base release: `v0.5.5` (`5c5d733c084f98e2ffb90336fc55bcac4f62bd7c`)

Validation ran against the fully resolved merge tree immediately before its
merge commit. No tracked content changed between the passing gates and commit
`f71f211`; temporary ignored dependencies were removed afterward.

## Environment

- macOS 26.5.2 (25F84)
- Node.js v26.0.0
- npm 11.12.1
- Pi 0.81.1 with authenticated provider configuration

## CI-equivalent gate

Command:

```bash
npm run verify:ci
```

Result: **passed**.

- Formatting and TypeScript checks passed.
- Unit/integration: 74 files passed; 576 tests passed; 2 todo.
- Main, preload, and renderer production builds passed.
- Site validation passed for v0.6.0.
- Deterministic Electron E2E: 58 passed; the 6 authenticated real-Pi tests were
  intentionally skipped by this fake/CI-equivalent command and run separately
  below.

The Electron suite included Unified Work launch/scope/origin/ownership/runtime
coverage, cross-workspace canonical sidebar resume, workspace lifecycle,
workflow coexistence, private task exclusion, and the v0.5.5 tooltip-hover
stabilization.

## Authenticated real-Pi gate

Command:

```bash
npm run test:e2e:real-smoke
```

Result: **passed — 6/6**.

The passing scenarios covered:

1. Parallel worker-model selection.
2. Agent Workflow persistence across restart.
3. Default-workspace prompt/resume and explicit legacy bridge compatibility.
4. Ordinary parent sessions omitting the legacy `deck_delegate` tool.
5. Draft Parallel opt-in routing from the first prompt.
6. Production planning of multiple private task sessions without bridge
   election.

No test was skipped in the authenticated command.

## Documentation captures

Before final validation, the candidate's current product screenshots were
regenerated and reviewed:

```bash
npm run docs:capture
PI_DECK_CAPTURE_REAL_PI=1 npm run docs:capture:real-workflows
npm run docs:render-social-preview
npm run check:site
```

The deterministic capture is accurately documented as real-mode UI driven by
production-shaped fake Pi RPC fixtures. Workflow and Parallel screenshots used
real local Pi workers. Mirrored `docs/assets` and `site/assets` files were
checked by the release-doc pass.

## Release ancestry

The candidate is a two-parent merge of the prior v0.6 integration tip and the
released `v0.5.5` commit. `git merge-base --is-ancestor v0.5.5 f71f211` passes,
so v0.6 now contains the tagged release's actual history rather than replayed
patches.

## Scope note

This record is committed immediately after the validated candidate and changes
only release evidence. The product, test, release metadata, and asset tree
validated above are those of `f71f211`.
