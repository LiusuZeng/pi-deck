# v0.6.4 Release Validation

Date: 2026-09-12 (PDT; validation completed 2026-09-13 UTC)
Validated candidate: `ea44454a2393a682431b7da25e8d495b9118635f`
Evidence branch: `docs/v0.6.4-release-validation`
Version: `0.6.4`
Base release: `v0.6.3` (`2f505051c4c39c3125d3bf3af3e2d842635290ee`)

Validation ran against the exact clean merged `main` candidate above. This
evidence branch was created after the annotated tag and GitHub Release, so the
evidence commit does not change the release tag target.

## Environment

- macOS 26.5.2 (25F84)
- Node.js v26.0.0
- npm 11.12.1
- isolated Pi 0.85.0 with authenticated provider configuration
- global Pi 0.81.1 was not changed

## Protected exact-main gate

The release-preparation PR passed the protected **Verify desktop app** check:

- PR: <https://github.com/LiusuZeng/pi-deck/pull/93>
- Run: <https://github.com/LiusuZeng/pi-deck/actions/runs/34729848990>
- Job: <https://github.com/LiusuZeng/pi-deck/actions/runs/34729848990/job/103650497921>

The exact merged-main candidate then passed the same protected check:

- Run: <https://github.com/LiusuZeng/pi-deck/actions/runs/34730206124>
- Job: <https://github.com/LiusuZeng/pi-deck/actions/runs/34730206124/job/103651469114>
- Result: **passed**

## Exact-candidate release gate

Command:

```bash
PI_DECK_PI_BINARY=<isolated-pi-0.85.0>/bin/pi npm run verify:release
```

Result: **passed** on `ea44454a2393a682431b7da25e8d495b9118635f`.

The committed, display-control-normalized
[`verify:release` log](artifacts/v0.6.4-ea44454-verify-release.log) is bound to
that candidate.

- SHA-256: `209983f2d0055ef7a8905fc025d49abafbc97be2894ef603a27083ab511fb35a`
- Started: 2026-09-13T01:18:49Z
- Finished: 2026-09-13T01:25:05Z
- Working tree before dependency install: clean
- Working tree before and after validation: clean
- Exit status: 0
- Unit/integration: 88 files passed; 768 tests passed; 2 todo
- Deterministic Electron E2E: 85 passed
- Authenticated real-Pi E2E: 8 passed; none skipped

Formatting, TypeScript checks, production builds, deep built-app validation,
site validation, real Pi RPC smoke, deterministic Electron E2E, and the full
authenticated real-Pi suite all passed.

The authenticated suite covered real model discovery, workflow persistence,
default-workspace prompt/resume, bridge isolation, command stdout/stderr/exit
status, ordinary parent tool isolation, first-prompt Parallel routing, task
progress while the parent remains active, and production multi-task planning
without bridge election.

## v0.6.4 scroll-follow acceptance

The candidate retained the trajectory assertion that originally exposed the
post-v0.6.3 failure. It does not replace the assertion with a final-position-only
check or a retry. The follow and manual-scroll scenarios were additionally run
20 times each against the exact candidate:

```bash
npx playwright test -c e2e/playwright.config.ts e2e/pi-deck.e2e.ts \
  --grep 'streaming Agent activity (follows bottom without bouncing|respects manual scroll intent)' \
  --repeat-each=20 --reporter=list
```

Result: **40 passed**, with no failure, retry, screenshot, trace, or error
context.

The committed
[`focused scroll log`](artifacts/v0.6.4-ea44454-scroll-repeat.log) has SHA-256
`85c16b3628ca895968cc0c8314320ba032744308db79d00f922a5f1232c6466c`.

This confirms both sides of the corrected ownership contract:

- stream-driven layout changes cannot revoke bottom-follow or expose an upward
  bounce while the user is following live output;
- explicit manual scroll intent preserves the user's reading position while
  subsequent Agent activity continues.

The full deterministic suite also passed expanded tool-detail reveal behavior,
covering coordination between explicit detail reveal and timeline follow.

## Parallel task-session acceptance

The deterministic and authenticated suites together confirmed the unchanged
release contract:

- Parallel Off routes prompts to the parent; Parallel On defaults to New task
  session with an explicit Work in parent override.
- Production routing performs model-backed planning before creating private
  task sessions; it does not depend on `deck_delegate` tool election.
- Multiple task sessions run or queue within the configured bound, remain
  parent-facing only, and do not leak into sidebar or Work rows.
- The flat task panel reports task status while the parent remains usable.
- Attachments are materialized without persisting private payloads.
- Terminal task results are synthesized into the parent.
- Restart preserves interrupted trace without resuming unfinished private work.
- Workspace/archive lifecycle guards and renderer projections remain covered by
  the deterministic acceptance suite.

## Playwright diagnostics review

The final candidate's deterministic, authenticated, and focused Playwright runs
had no failures or retries. `test-results` contained only Playwright's successful
`.last-run.json`; no failure screenshots, traces, or error contexts required
disposition.

The earlier main failure at
<https://github.com/LiusuZeng/pi-deck/actions/runs/34712365268> was inspected,
reproduced locally, diagnosed as a product race, and fixed through protected PR
[#92](https://github.com/LiusuZeng/pi-deck/pull/92). Its assertion was preserved.
It is not a failed run of this candidate.

## Publication

- Annotated tag: `v0.6.4`
- Tag target: `ea44454a2393a682431b7da25e8d495b9118635f`
- GitHub Release: <https://github.com/LiusuZeng/pi-deck/releases/tag/v0.6.4>
- Release state: published, non-draft, non-prerelease, latest
- Pages deployment: <https://github.com/LiusuZeng/pi-deck/actions/runs/34730206110>
- Live site: <https://liusuzeng.github.io/pi-deck/> displays `v0.6.4`

## Release assertion

The annotated `v0.6.4` tag points exactly to the clean, protected-CI-green,
authenticated candidate `ea44454a2393a682431b7da25e8d495b9118635f`.
This later evidence commit must not change that tag target.
